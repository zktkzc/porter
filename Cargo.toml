[package]
name = "porter"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = {version = "4.5.21", features = ["derive", "wrap_help"]}
tokio = {version = "1.41.1", features = ["rt-multi-thread", "full"]}
cidr = "0.3.0"